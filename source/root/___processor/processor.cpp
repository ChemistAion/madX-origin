#include "_root.hpp"



namespace madX::Root
{
	void Processor::ProcessNodes(const Meta::State& state)
	{
	//	const auto& nodes_state = (const Panel::NodesEditorState&)
	//	const auto& nodes = state.nodes_;
	//	
	//	{
	//		bool dirty = false;
	//	
	//		for (const auto& node : nodes)
	//			dirty |= node->state_ & Meta::Nodes::NodeStateFlag_Dirty ? true : false;
	//	
	//		IM_ASSERT(false == dirty);
	//	}

		////////////////////////////////////////////////////////////////////////////////
	}
}
